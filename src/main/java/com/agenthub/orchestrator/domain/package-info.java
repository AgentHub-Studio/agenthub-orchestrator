/**
 * Domain models and value objects
 * 
 * Contains:
 * - Pipeline definitions (DAG structure)
 * - Execution state (runtime state)
 * - Node definitions and results
 * - Enums (NodeType, ExecutionStatus, etc.)
 * 
 * Domain model is immutable where possible.
 * Follows DDD principles (aggregates, value objects, entities).
 * 
 * @since 1.0.0
 */
package com.agenthub.orchestrator.domain;
